import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Route Handler proxies for CORS-blocked APIs (AWS, Oracle)
  // will be added under app/api/ in Phase 4+
};

export default nextConfig;
