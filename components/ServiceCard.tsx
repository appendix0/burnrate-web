// Phase 3 — dashboard card per service
import { ServiceType } from "@/lib/constants/services";

interface ServiceCardProps {
  service: ServiceType;
}

export function ServiceCard({ service: _service }: ServiceCardProps) {
  // Phase 3 implementation
  return null;
}
