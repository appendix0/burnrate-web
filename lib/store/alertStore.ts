// Phase 8 — Zustand store for budget threshold alerts
import { BudgetAlert } from "@/lib/models/budgetAlert";

export type AlertStoreState = {
  alerts: BudgetAlert[];
};

// Store implementation — Phase 8
